package numberPrograms;

public class spyOrNot {

	public static void main(String[] args) {
		
		int n=123;
		int sum=0;
		int prod=1;
		while(n>0)
		{
			int r=n%10;
			sum+=r;
			prod*=r;
			n/=10;
		}
		if(sum==prod) System.out.println("neon");
		else System.out.println("not");

	}

}
